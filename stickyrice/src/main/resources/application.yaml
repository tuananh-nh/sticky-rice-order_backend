server:
  port: 8088

spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=stickyrice2;encrypt=false
    username: sa
    password: 858685
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
